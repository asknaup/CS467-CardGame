<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, intial-scale=1.0">
  <title>Card View Page</title>
  <link rel="stylesheet" href="global.css">
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <h1>
    Inspect the Card
  </h1>
  <div>
    <form id="imageForm" action="/cardViewPrintedPage" method="POST">
      <img src="{{ val.[0].url }}" alt="Image">
      <input type="hidden" name="cardName" value="{{ cardName }}">
      <input type="hidden" name="cardType" value="{{ cardType }}">
      <input type="hidden" name="rarity" value="{{ rarity }}">
      <input type="hidden" name="manaCost" value="{{ manaCost }}">
      <input type="hidden" name="url" value="{{ val.[0].url }}">
      <input type="submit" value="Print">
    </form>
    <form id="imageForm" action="/cardViewPrintedPage" method="POST">
      <img src="{{ val.[1].url }}" alt="Image">
      <input type="hidden" name="cardName" value="{{ cardName }}">
      <input type="hidden" name="cardType" value="{{ cardType }}">
      <input type="hidden" name="rarity" value="{{ rarity }}">
      <input type="hidden" name="manaCost" value="{{ manaCost }}">
      <input type="hidden" name="url" value="{{ val.[1].url }}">
      <input type="submit" value="Print">
    </form>
    <form id="imageForm" action="/cardViewPrintedPage" method="POST">
      <img src="{{ val.[2].url }}" alt="Image">
      <input type="hidden" name="cardName" value="{{ cardName }}">
      <input type="hidden" name="cardType" value="{{ cardType }}">
      <input type="hidden" name="rarity" value="{{ rarity }}">
      <input type="hidden" name="manaCost" value="{{ manaCost }}">
      <input type="hidden" name="url" value="{{ val.[2].url }}">
      <input type="submit" value="Print">
    </form>
  </div>
  <button>Edit</button>
  <button>Delete</button>
</body>
</html>